spring:
  application:
    name: "DevOpsAutomation"
  task:
    scheduling:
      pool:
        size: 10
  datasource:
    username: root
    password: rootroot
    url: "jdbc:mysql://localhost:3306/developmentAutoLog"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: devwhenry@gmail.com
    password: dmkj mopg cmgs rjoj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  file:
    name: "log/service.log"

server:
  port: 9100

