spring:
  application:
    name: "DevOpsAutomation"
  task:
    scheduling:
      pool:
        size: 10
  datasource:
    username: root
    password: rootroot
    url: "jdbc:mysql://localhost:3306/AutoLogMonitor"
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      database-platform: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: devwhenry@gmail.com
    password: mrdl qzen quja xekx
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  file:
    name: "log/service.log"

server:
  port: 9100

